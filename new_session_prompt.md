# New Session Prompt — blackjack-vision

Paste everything below into a fresh Claude Code session opened in the project root
(`C:\Users\Cam\Documents\.ClaudeCode Projects\blackjack-vision`). In Claude Code you can just
type `/resume` instead — this file is the fallback for any other environment.

---

Resume the blackjack-vision project. Do this, in order:

1. Read `CLAUDE.md` (project rules), then `handoff.md` (current state), then open the active
   feature file named in handoff.md's **🔗 Pointer** section.
2. Summarize your understanding back to me in exactly 3 bullets:
   (a) where the project stands, (b) what the active feature is, (c) what you'd do next.
3. Confirm the **➡️ Next Up** items from handoff.md with me before doing any work — I may
   redirect you.

Rules that always apply: follow the verification protocol in CLAUDE.md (no `verified done`
without a Verification Log entry), respect the truth hierarchy (code > handoff > stage files >
master plan), and when I say "update all relevant files", run the `/sync-docs` procedure.
