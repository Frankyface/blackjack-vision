# /resume — reboot a fresh session from the handoff

Do this, in order:

1. Read `CLAUDE.md` (project rules), then `handoff.md` (current state), then open the active
   feature file named in handoff.md's **🔗 Pointer** section.
2. Summarize your understanding back to Cam in exactly 3 bullets:
   (a) where the project stands, (b) what the active feature is, (c) what you'd do next.
3. Confirm the **➡️ Next Up** items from handoff.md with Cam before doing any work — he may
   redirect you.

Always-on rules: verification protocol from CLAUDE.md (no `verified done` without a
Verification Log entry), truth hierarchy (code > handoff.md > stage files > master plan), and
"update all relevant files" triggers `/sync-docs`.
